## Web

React frontend for Inferno-Tactix.

Run locally:

```bash
npm install
npm start
```

Environment variables:

- `API_BASE_URL` default `http://localhost:6969`
- `WS_URL` default `ws://localhost:8765`
- `GENERATED_ASSETS_BASE_URL` default `/generated`
